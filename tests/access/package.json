{
  "private": true,
  "type": "module",
  "scripts": {
    "test": "firebase emulators:exec --project demo-marcopolo-access --only firestore --config firebase.json \"node --test access.test.mjs\"",
    "pretest": "node -e \"require('node:fs').copyFileSync('../../firestore.rules','firestore.rules')\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "5.0.0",
    "firebase": "12.7.0",
    "firebase-tools": "14.17.0",
    "typescript": "5.9.3",
    "@playwright/test": "1.55.0"
  }
}
